const getters = {
	skipPage: state => state.app.skipPage,
	isSkip: state => state.app.isSkip,
	collapsed: state => state.app.collapsed,
	leftNavState: state => state.app.leftNavState
}

export default getters